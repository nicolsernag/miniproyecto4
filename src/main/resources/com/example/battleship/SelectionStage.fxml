<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.ColumnConstraints?>
<?import javafx.scene.layout.GridPane?>
<?import javafx.scene.layout.Pane?>
<?import javafx.scene.layout.RowConstraints?>

<AnchorPane prefHeight="617.0" prefWidth="913.0" xmlns="http://javafx.com/javafx/24.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.example.battleship.controller.SelectionController">

    <children>

        <!-- === TABLERO DEL JUGADOR (10x10) === -->
        <GridPane fx:id="playerGrid" gridLinesVisible="false" layoutX="44.0" layoutY="100.0" prefHeight="400" prefWidth="400">

            <columnConstraints>
                <ColumnConstraints prefWidth="40" />
                <ColumnConstraints prefWidth="40" />
                <ColumnConstraints prefWidth="40" />
                <ColumnConstraints prefWidth="40" />
                <ColumnConstraints prefWidth="40" />
                <ColumnConstraints prefWidth="40" />
                <ColumnConstraints prefWidth="40" />
                <ColumnConstraints prefWidth="40" />
                <ColumnConstraints prefWidth="40" />
                <ColumnConstraints prefWidth="40" />
            </columnConstraints>

            <rowConstraints>
                <RowConstraints prefHeight="40" />
                <RowConstraints prefHeight="40" />
                <RowConstraints prefHeight="40" />
                <RowConstraints prefHeight="40" />
                <RowConstraints prefHeight="40" />
                <RowConstraints prefHeight="40" />
                <RowConstraints prefHeight="40" />
                <RowConstraints prefHeight="40" />
                <RowConstraints prefHeight="40" />
                <RowConstraints prefHeight="40" />
            </rowConstraints>

        </GridPane>

        <!-- === PANEL DE BARCOS === -->
        <Pane fx:id="shipPanel" layoutX="500.0" layoutY="20.0" prefHeight="520.0" prefWidth="374.0">
        </Pane>

        <!-- === BOTÓN CONTINUAR === -->
        <Button fx:id="continueButton" layoutX="734.0" layoutY="551.0" text="Continuar" />

    </children>
</AnchorPane>
